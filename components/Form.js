import styled from 'styled-components'

const Form = styled.form`
  padding: 20px;
  fieldset {
    border: none;
  }
  label {
    display: block;
    margin-bottom: 10px;
    input {
      display: block;
    }
  }
  Button {
    margin-right: 10px;
  }
`;

export default Form
